# Metadata microservice

> Rust rest API built with Axum

## Usage

> Don't forget to have a well configured `mise.local.toml`

Locally:
```bash
cargo run
```

Or with docker:
```bash
mise run server-start
```
> See `mise.toml` for more commands
